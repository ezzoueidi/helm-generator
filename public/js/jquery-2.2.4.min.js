var counter = 0;
window.addEventListener("load", function() {
  document.getElementById("add").addEventListener("click", function() {
    // Create a div
    var div = document.createElement("div");

    // Create a text input
	  var texte = document.createTextNode("    Chart Name:  ");
   		 var text = document.createElement("input");
    		text.setAttribute("type", "text");
    		text.setAttribute("name", "chartt" + counter++); // You may want to change this
	  // stuck here ^
	  	
	  var front = document.createTextNode("    Frontend:  ");
	  var back = document.createTextNode("    Backend:   ");
	   var chart_type = document.createElement("input");
	  chart_type.setAttribute("type", "radio");
	  chart_type.setAttribute("name", "chart_type");
	  var chart_type1 = document.createElement("input");
	  chart_type1.setAttribute("type", "radio");
	  chart_type1.setAttribute("name", "chart_type1");


    // Create a text input
    //var text = document.createElement("input");
    //text.setAttribute("type", "text");
    //text.setAttribute("name", "text"); // you may want to change this

    // add the file and text to the div
    //div.appendChild(file);
	  div.appendChild(texte);
    div.appendChild(text);
	  div.appendChild(front);
	  div.appendChild(chart_type);
	  div.appendChild(back);
	  div.appendChild(chart_type1);
	  

    //Append the div to the container div
    document.getElementById("container_append").appendChild(div);
  });

	document.getElementById("add").addEventListener("click", function() {
		var div = document.createElement("div");

		var text = document.createElement("input");
		text.setAttribute("", "");
		text.setAttribute("", "");

		div.appendChild(text);
		document.getElementById("").appendChild(div);

	});
});

function yesnoCheck() {
    if (document.getElementById('yesCheck').checked) {
        document.getElementById('ifYes').style.display = 'block';
    }
    else document.getElementById('ifYes').style.display = 'none';

}
